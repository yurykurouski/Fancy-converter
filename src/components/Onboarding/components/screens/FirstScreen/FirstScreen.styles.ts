import { StyleSheet } from 'react-native';

export const screenStyles = StyleSheet.create({
  appIcon: {
    alignSelf: 'center',
  },
  iconBack: {
    borderRadius: 80,
    justifyContent: 'center',
    elevation: 10,
    alignSelf: 'center',
    marginTop: '50%',
  },
});
