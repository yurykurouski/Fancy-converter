import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  gradientContainer: {
    right: 10,
    borderRadius: 15,
    height: 64,
    position: 'absolute',
    width: '100%',
  },
});
