import React from 'react';
import { StyleSheet, View } from 'react-native';

const styles = StyleSheet.create({
  container: { marginBottom: 60 },
});

export const BottomSheetFooterComponent = () => (
  <View style={styles.container} />
);
