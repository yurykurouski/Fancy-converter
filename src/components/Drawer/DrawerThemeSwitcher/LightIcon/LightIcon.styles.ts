import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  core: {
    width: '50%',
    height: '50%',
  },
  rays: {
    width: '100%',
    height: '100%',
    zIndex: 1,
    resizeMode: 'cover',
    position: 'absolute',
  },
});
