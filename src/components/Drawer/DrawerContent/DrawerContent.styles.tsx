import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  iconsContainer: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    padding: 5,
    marginTop: 'auto',
  },
});
